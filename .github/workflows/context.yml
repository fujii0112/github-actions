name: Contexts
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      WORKSPACE: ${{github.workspace}}
      RUNNER_OS: ${{runner.os}}
    steps:
      - run: echo "${WORKSPACE} ${RUNNER_OS}"
